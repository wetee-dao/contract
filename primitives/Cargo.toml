[package]
name = "polkadot-primitives"
version = "0.1.0"
edition = "2021"
description = "PolkaVM 合约用轻量 primitives（ensure!/ok_or_err!）"

[lib]
name = "polkadot_primitives"
path = "src/lib.rs"

[dependencies]
